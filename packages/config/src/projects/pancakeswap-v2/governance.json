{
  "framework": "Snapshot (off-chain) + 3-of-7 multisig execution",
  "voteExecution": "offchain",
  "votingUnit": "CAKE token (Snapshot signal); 3-of-7 GnosisSafe signers (binding)",
  "proposalRequirements": "Community Snapshot proposals require 100K CAKE wallet balance at snapshot block; on-chain execution requires 3 of 7 multisig signatures.",
  "votingProcess": "Off-chain Snapshot vote (3-day window for community proposals); 3-of-7 multisig executes if approved. No on-chain timelock.",
  "proposalPeriod": {
    "kind": "fixed",
    "value": "3 days (community proposals via Snapshot)"
  },
  "executionDelay": {
    "kind": "none"
  }
}
